option '-p', '--port NUMBER',     'port to listen on'
option '-a', '--address ADDRESS', 'address to listen at'
option '-S', '--socket PATH',     'unix socket to bind to'
option '-F', '--fake',            'disable APIs and use fake data'

task 'serve', 'serve the site for development', (options) ->
  require('./source/Server/Server.coffee').serve(test: true)

task 'install', 'install requirements for development', (options) ->
  console.log "do something here"


task 'build', 'build the site for production', (options) ->

task 'push', 'push the site to production', (options) ->


task 'test', 'test', (options) ->
  task 'sub', (options) ->
    console.log "YEAH"


