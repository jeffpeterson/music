#logo.icon-music

%input#search{type: "text", placeholder: "search . . ."}

- if App.adapters.rdio._get('isAuthenticated')
  %a{href: '#collection'         } collection
- else
  %a{href: '#', class: 'sign-in'} sign in

%a{href: '#catalog/top-charts' } top charts

%button#show-sidebar.slidable.icon-chevron-left
